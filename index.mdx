---
title: "WAT Rooms"
description: "Book WAT meeting rooms in seconds, from the web app, the terminal, or your AI coding agent."
---

WAT Rooms is the booking app for the WAT community's meeting rooms. See live availability, reserve a free time slot, and manage your own bookings.

There is a web app at [wat-app.vercel.app](https://wat-app.vercel.app), but WAT Rooms is built to be driven by **AI coding agents** (Claude Code, Codex, and friends). A small `wat` CLI exposes the same API the web app uses, and a skills plugin lets your agent book rooms conversationally.

<Note>
**For agents:** the fastest path is the [Quickstart for agents](/quickstart). Install the `wat` CLI, install the `wat` skills plugin, run `wat login` once, then just ask your agent to book a room. Everything after the login step happens in natural language.
</Note>

## Pick your path

<CardGroup cols={2}>
  <Card title="Quickstart for agents" icon="wand-sparkles" href="/quickstart">
    Install the CLI and skills, log in, and have your agent book a room.
  </Card>
  <Card title="Create an account" icon="user-plus" href="/account">
    Self-enroll with your email and the WAT signup code. Passwordless.
  </Card>
  <Card title="The wat CLI" icon="terminal" href="/cli">
    Install the CLI and the full command reference: rooms, availability, bookings.
  </Card>
  <Card title="Skills plugin" icon="puzzle" href="/plugin">
    The four `wat:` skills your agent uses to book rooms in plain language.
  </Card>
  <Card title="Booking rules" icon="book-open" href="/booking-rules">
    Durations, the daily budget, the booking horizon, privacy, and emails.
  </Card>
  <Card title="Open the app" icon="calendar-check" href="https://wat-app.vercel.app">
    The web app: a calendar view of every room, with one-click booking.
  </Card>
</CardGroup>

## How it fits together

One account works everywhere. You sign up once in the web app, mint an API key, and the same identity drives the web app, the `wat` CLI, and the skills plugin.

- **Web app:** a calendar with one column per room, point and click to book.
- **`wat` CLI:** book from the terminal, scriptable, machine-readable with `--json`.
- **Skills plugin:** thin wrappers over the CLI so an agent can book for you in natural language.

The CLI and the skills call the **same** public API the web app uses, so every rule (availability, your daily budget, privacy) is enforced identically no matter how you book.
